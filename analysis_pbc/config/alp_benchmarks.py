"""
config/alp_benchmarks.py

Physics Beyond Colliders (PBC) ALP benchmark scenarios.

These benchmarks define the *coupling structure* (Wilson coefficients) used by
the ALP model and production branching ratio calculations.
"""

from __future__ import annotations

from typing import Dict, Any


PBC_BENCHMARKS: Dict[str, Dict[str, Any]] = {
    "BC9": {
        "name": "photon-coupled",
        "description": "ALP coupled dominantly to photons",
        "C_gg": 1.0,  # Photon Wilson coefficient
        "C_GG": 0.0,  # Gluon coefficient
        "C_WW": 0.0,  # W coefficient
        "c_ff": 0.0,  # Fermion coefficients (universal)
        "dominant_decay": "gamma_gamma",
        "production_channels": ["h_to_Za", "Z_to_gamma_a", "Primakoff"],
        "mass_range_GeV": [1e-6, 62.5],
        "fa_range_GeV": [1e2, 1e15],
        "note": "Primakoff irrelevant for transverse detectors",
    },
    "BC10": {
        "name": "fermion-coupled",
        "description": "ALP with universal fermion couplings",
        "C_gg": 0.0,
        "C_GG": 0.0,
        "C_WW": 0.0,
        "c_ff": 1.0,  # Universal: c_e = c_mu = c_tau = c_u = c_d = ... = 1
        "dominant_decay": "leptons_or_hadrons",
        "production_channels": ["B_to_Ka", "K_to_pi_a", "h_to_aa", "Drell_Yan"],
        "mass_range_GeV": [1e-3, 10.0],
        "fa_range_GeV": [1e2, 1e9],
        "note": "Best for transverse detectors - charged final states",
    },
    "BC11": {
        "name": "gluon-coupled",
        "description": "ALP coupled dominantly to gluons",
        "C_gg": 0.0,  # Loop-induced: C_gg_eff ≈ 1.92 × (α_em/α_s) × C_GG
        "C_GG": 1.0,
        "C_WW": 0.0,
        "c_ff": 0.0,  # Generated by RG running
        "dominant_decay": "hadrons",
        "production_channels": ["gluon_fusion", "meson_mixing", "h_to_aa"],
        "mass_range_GeV": [0.3, 10.0],
        "fa_range_GeV": [1e2, 1e9],
        "note": "Loop-induced γγ enables photon detection",
    },
}


def get_alp_benchmark(name: str) -> Dict[str, Any]:
    """
    Return a benchmark definition by name.

    Parameters
    ----------
    name : str
        One of: "BC9", "BC10", "BC11"
    """
    if name not in PBC_BENCHMARKS:
        raise ValueError(f"Unknown ALP benchmark: {name} (use BC9, BC10, BC11).")
    return PBC_BENCHMARKS[name]

